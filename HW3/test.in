void main() {
    int x = 1;
    byte y = x + 1;
    printOk((x > y) and (x < 100));
    int z;
}

// test comment
void printOk(bool isOk) {
    int i = 0;
    while (i < 10) {
        print("i = ");
        printi(i);
        i = i + 1;
    }
    if (isOk) {
        int x = 0;
        print("ok");
    } else print("not ok");
}